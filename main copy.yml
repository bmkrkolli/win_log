---
- hosts: all
  gather_facts: yes
  tasks:
    - name: populate service facts
      service_facts:
      no_log: true
    - name: "Restart Service"
      service: 
        name: "{{ service_name }}"
        state: restarted
      register: status
      become: yes
