---
- name: Generate treaddump.
  hosts: all
  gather_facts: false
  tasks:
  - name: get PID.
    shell: "ps aux | head -2 | awk  -F '[ ]*' '{print $2}'"
    register: pid
  - debug:
     var: pid.stdout